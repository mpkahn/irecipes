<div class="post">
  <article class="w3-card-4">
    <div class="w3-container top-card">
      <h2>{{post.title}}</h2>
      <div>
        Posted by {{post.User.username}} on {{format_date post.createdAt}}
      </div>
    </div>
    <div class="card-body">
      <p>Ingredients</p>
      {{post.body}}
      <br>
      <p>Instructions</p>
      {{post.content}}
    </div>
  </article>

  {{#if post.Comments}}
    <section class="w3-container top-card">
      <div>
        <h3>Comments</h3>
      </div>

      <div>
        {{#each post.Comments}}
          <div>
            <p>
              {{body}}
            </p>
            <p>
              &mdash; {{User.username}}, {{format_date createdAt}}
            </p>
          </div>
        {{/each}}
      </div>
    </section>
  {{/if}}

  <br>

  <form id="new-comment-form" class="w3-card-4">
    <div class="w3-container top-card">
      <h3>Add New Comment</h3>
    </div>

    <div class="card-body">
      <input type="hidden" name="post-id" value="{{post.id}}" />

      <label class="form-label" for="comment-body">Comment</label>

      <textarea name="comment-body" class="form-input"></textarea>

      <button type="submit" class="btn">Submit</button>
    </div>
  </form>
</div>

<script src="/js/comment.js"></script>