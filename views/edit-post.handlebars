<div class="w3-card-4 ">

<header class="w3-container top-card editcard">

  
  
  <h1>
  Edit Post
  </h1>
</header> 

 <form id="edit-post-form" class="card-body">
    <input type="hidden" name="post-id" value="{{post.id}}" />

    <label class="form-label" for="post-title">Title</label>
    <input type="text" name="post-title" value="{{post.title}}" class="form-input" />

    <label class="form-label" for="post-body">Ingredients</label>
    <textarea name="post-body" class="form-input">{{post.body}}</textarea>

    
    <label class="form-label" for="post-content">Instructions</label>
    <textarea name="post-content" class="form-input">{{post.content}}</textarea>

  

    <button type="submit" class="btn editcard">Update</button>

    <button type="button" id="delete-btn" class="btn editcard">Delete</button>
    </form>
{{!-- </footer> --}}

  {{!-- <div class="post card">
  <div class="card-header">
    <h2>Edit Post</h2>
  </div>
   --}}
{{!--   
  <form id="edit-post-form" class="card-body">
    <input type="hidden" name="post-id" value="{{post.id}}" />

    <label class="form-label" for="post-title">Title</label>
    <input type="text" name="post-title" value="{{post.title}}" class="form-input" />

    <label class="form-label" for="post-body">Ingredients</label>
    <textarea name="post-body" class="form-input">{{post.body}}</textarea>

    
    <label class="form-label" for="post-content">Instructions</label>
    <textarea name="post-content" class="form-input">{{post.content}}</textarea>

  

    <button type="submit" class="btn">Update</button>

    <button type="button" id="delete-btn" class="btn">Delete</button>
  </form>
</div> --}}

<script src="/js/edit.js"></script>